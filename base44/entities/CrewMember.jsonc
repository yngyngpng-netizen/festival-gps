{
  "name": "CrewMember",
  "type": "object",
  "title": "Crew Member",
  "description": "A festival friend in a shared group with profile pin, group code, and parsed schedule.",
  "properties": {
    "userId": {
      "type": "string",
      "maxLength": 120
    },
    "groupCode": {
      "type": "string",
      "maxLength": 24
    },
    "name": {
      "type": "string",
      "maxLength": 80
    },
    "email": {
      "type": "string",
      "format": "email",
      "maxLength": 160
    },
    "photo": {
      "type": "string",
      "maxLength": 800000
    },
    "color": {
      "type": "string",
      "maxLength": 16
    },
    "schedule": {
      "type": "array",
      "default": [],
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "maxLength": 120
          },
          "artist": {
            "type": "string",
            "maxLength": 160
          },
          "stageId": {
            "type": "string",
            "maxLength": 40
          },
          "day": {
            "type": "string",
            "enum": ["friday", "saturday", "sunday"]
          },
          "start": {
            "type": "integer",
            "minimum": 0,
            "maximum": 3000
          },
          "end": {
            "type": "integer",
            "minimum": 0,
            "maximum": 3000
          }
        },
        "required": ["artist", "stageId", "day", "start", "end"]
      }
    },
    "liveLocation": {
      "type": "object",
      "description": "Fresh phone GPS location projected onto the EDC map. Schedule is used when this is missing or stale.",
      "properties": {
        "lat": {
          "type": "number",
          "minimum": -90,
          "maximum": 90
        },
        "lon": {
          "type": "number",
          "minimum": -180,
          "maximum": 180
        },
        "x": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "y": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "accuracy": {
          "type": "number",
          "minimum": 0
        },
        "updatedAt": {
          "type": "string"
        },
        "online": {
          "type": "boolean"
        },
        "source": {
          "type": "string",
          "maxLength": 24
        },
        "stageId": {
          "type": "string",
          "maxLength": 40
        }
      }
    }
  },
  "required": ["userId", "groupCode", "name"],
  "rls": {
    "create": true,
    "read": {
      "data.groupCode": "{{user.data.festivalGroupCode}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}
